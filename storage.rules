rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // for emulator demos you can relax to true; tighten for prod
      allow read, write: if true;
      // Better (auth required):
      // allow read, write: if request.auth != null;
    }
  }
}
